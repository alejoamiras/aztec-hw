# Fuzzer + replay-oracle binaries + libFuzzer-generated artifacts (regenerated by `make`).
fuzz_authwit
fuzz_append_call
fuzz_deploy_parse
replay_authwit
replay_append_call
replay_deploy_parse
*.dSYM/
# libFuzzer crash/leak/timeout repros (a real finding gets a committed regression
# test in wire-negative.test.ts, not a blob here).
crash-*
leak-*
timeout-*
oom-*
# Generated coverage corpus + harvested reject reservoir (regenerated by a fuzz
# run with WIRE_RESERVOIR set). Deliberate SEED inputs are committed under
# corpus/seeds/ instead.
corpus/
reservoir/
