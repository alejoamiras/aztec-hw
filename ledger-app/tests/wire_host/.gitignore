# Fuzzer binaries + libFuzzer-generated artifacts (regenerated by `make`).
fuzz_authwit
fuzz_append_call
fuzz_deploy_parse
*.dSYM/
# libFuzzer crash/leak/timeout repros (a real finding gets a committed regression
# test in wire-negative.test.ts, not a blob here).
crash-*
leak-*
timeout-*
oom-*
# Generated coverage corpus. Deliberate SEED inputs (if any) are committed under
# corpus/seeds/ instead.
corpus/
