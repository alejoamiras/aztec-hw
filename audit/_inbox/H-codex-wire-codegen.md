<!-- Harvested from codex session 019e89c4 (wire-protocol/codegen-provenance), read-only xhigh. -->

### F-H-1: Deploy-profile sponsor call is manifest-trusted, hidden in UI, and never canonical-verified
- Severity: HIGH — a poisoned release can keep the deploy screen at `Sponsored (testnet)` while making the device sign a different sponsor target/selector; there is no CI or runtime equality check on those hidden signed fields.
- Owned: OURS — codegen pipeline + deploy review flow
- Category: BUILD
- Location: packages/adapter-ledger/scripts/gen-clear-signing-v0.ts:466-530,577-595; packages/adapter-ledger/clear-signing-v0/manifest.json:151-154,166-169; ledger-app/src/handler/finalize_deploy_and_sign.c:191-212; ledger-app/src/ui/deploy_review_ui.c:106-112; ledger-app/src/l4/account_binding.c:57-71; packages/adapter-ledger/src/deploy-outer-hash-parity.test.ts:96-107; packages/adapter-ledger/src/aztec-ledger-session.ts:375-392
- What / Attack-impact / Evidence (quote code you read) / Fix sketch / Confidence: What: `crossCheckDeployProfile` only proves `account_class_id`, `ctor_selector_u32`, `ctor_arg_schema`, and `ctor_arg_byte_len`; it never checks `deployer`, `sponsor_fpc_address`, or `sponsor_selector_u32`. Attack-impact: in the current stock path, a C-table-only drift mostly fails closed because the host canonical deploy payload comes from the live framework call `new SponsoredFeePaymentMethod(this.deps.sponsoredFpcAddress)` (`aztec-ledger-session.ts:375-392`), but a poisoned build/release that aligns host config and generated tables can ship a Ledger app that signs a different sponsor call than the user is shown. Evidence: the checker comment explicitly scopes itself to class-id/ctor checks (`gen-clear-signing-v0.ts:466-477`), then the emitter blindly serializes `.deployer`, `.sponsor_fpc_address`, `.sponsor_selector_u32`, `.fee_mode` (`:577-595`); the device later signs `az_deploy_compute_outer_hash(... profile->sponsor_fpc_address, profile->sponsor_selector_u32, ...)` (`finalize_deploy_and_sign.c:199-205`), while the UI renders only `snprintf(g_fee_str, ..., "Sponsored (testnet)")` (`deploy_review_ui.c:106-107`). The same unchecked class includes `profile->deployer`, consumed in `account_binding_deploy_partial(... profile->deployer, ...)` (`account_binding.c:57-71`). The parity test does not bind the generated table: it recomputes `sponsorSelector` from `FunctionSelector.fromSignature('sponsor_unconditionally()')` and asserts `0x23d77f89`, never reading `CS_DEPLOY_PROFILES` (`deploy-outer-hash-parity.test.ts:96-107`). Fix sketch: fail-closed at codegen on canonical equality for `sponsor_selector_u32`, `sponsor_fpc_address`, and `deployer` against the real deploy path / committed deployment source; hash-pin them in `_meta`; make the parity test consume the generated profile table; show at least the sponsor FPC checksum/address on the deploy review screen. Confidence: high.
- Dedup-check: nearest AHW-035 and F-D-2, but distinct. AHW-035 is the host-side “mutable node_modules / comment-only pin” provenance gap; this is the device/display consequence for deploy-profile literals that are outside that artifact cross-check entirely. F-D-2 is dead `fee_mode` metadata; this is about live `sponsor_*` / `deployer` fields that enter the signed deploy path.

**Confirmed clean**
- `BEGIN_AUTHWIT` wire order matches exactly, including the v3 `profile_id|salt` insertion after the path and trailing-byte rejection: `packages/adapter-ledger/src/l4-manifest.ts:245-277` ↔ `ledger-app/src/handler/begin_authwit.c:32-106`.
- `APPEND_CALL` framing matches exactly: host emits `args_hash|selector|target|flags|args_count|args[]` in `packages/adapter-ledger/src/l4-manifest.ts:281-308`; device parses the same sequence, masks flags to `0x07`, bounds `args_count`, and rejects trailing bytes in `ledger-app/src/handler/append_call.c:93-124`.
- Short-APDU sizing is consistent end to end: the call body max is `226` bytes in `ledger-app/src/l4/wire.h:61-66`, and both transports enforce `data.length <= 255` in `packages/adapter-ledger/src/speculos-transport.ts:129-141` and `packages/adapter-ledger/src/webhid-transport.ts:124-136`.
- Public/private/empty-call `args_hash` rules line up exactly: host `encodeRealCall` uses public=`[selector,...args]` / `PUBLIC_CALLDATA`, private-empty=`0`, private-non-empty=`args` / `FUNCTION_ARGS` in `packages/adapter-ledger/src/l4-manifest.ts:67-79`; device does the same in `ledger-app/src/clear_signing_v0/args_hash.c:13-42`.
- The device does not carry host-claimed per-call hashes past parse time: `ledger-app/src/handler/append_call.c:160-181` double-recomputes `args_hash`, compares it to the host claim, then overwrites `slot->args_hash` with the device-authored value before `parity.c` / `finalize_and_sign.c` consume it.
- Outer-hash padding semantics match exactly: both sides hash `5 calls × 6 fields + tx_nonce`, with padding `selector=0`, `target=0`, `is_public=true`, `hide_msg_sender=false`, `is_static=false` in `packages/adapter-ledger/src/l4-manifest.ts:174-193` and `ledger-app/src/l4/parity.c:35-136`.
- `BEGIN_DEPLOY_ACCOUNT` wire order matches exactly: `packages/adapter-ledger/src/deploy-context.ts:73-108` ↔ `ledger-app/src/handler/begin_deploy_account.c:63-156`.
- Deploy finalization is device-authored before signing: `ledger-app/src/handler/finalize_deploy_and_sign.c:165-224` re-derives partial/address, recomputes the deploy outer hash locally, compares it to the host claim, and signs the local value.
- I did not find a fresh host/device divergence on the currently shipped separator/flag literals: `packages/adapter-ledger/src/l4-manifest.ts:37-40`, `packages/adapter-ledger/src/apdu.ts:184-201` are in sync with `ledger-app/src/l4/wire.h:70-85`. The missing equality guard for those literals is already AHW-084.